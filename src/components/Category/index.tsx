export default function Category({ name }) {
  function capitalizeFirstLetter(){
    const category = name
  }

    return name[0].toLocaleUpperCase()  + name.substring(1);
    // método para Capitalizar a propriedade name
  }